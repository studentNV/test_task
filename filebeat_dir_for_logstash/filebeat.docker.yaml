filebeat.inputs:
- type: log
  paths:
    - "./test1.log"

filebeat.autodiscover:
  providers:
    - type: docker
      hints.enabled: true

output.logstash:
   hosts: ["logstash770:5400"] 

# output.elasticsearch:
#   hosts: 'elasticsearch770:9200'
